{
  "name": "livestock-farm-management",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack livestock farm management system (MERN): Express + MongoDB backend with Clerk authentication and a React (Vite) frontend.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "npm install && npm --prefix client install && npm --prefix client run build",
    "seed:onehealth": "node scripts/seedOneHealthDemo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@clerk/express": "^1.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.9.0"
  }
}
